# Codebase Map

> Auto-generated by `packages/local-bin/bin/gen-codebase` — do not edit by hand. Regenerate with
> `just codebase` (or run `gen-codebase`) whenever the structure changes. Generated: 2026-08-17
> 07:39 UTC

> Purpose: compact orientation map for AI agents and quick human reference. Read this first instead
> of grepping the whole tree.

## Repo layout

| Path           | Purpose                                                        |
| -------------- | -------------------------------------------------------------- |
| `docs/`        | Documentation hub (manual, howtos, runbooks)                   |
| `files/`       | Dotfiles, shell config, static user files                      |
| `flake/`       | Flake internals (nixos.nix, checks, devshells)                 |
| `hosts/`       | Per-host entry points (hosts/<host>/default.nix)               |
| `lib/`         | Custom Nix library (mkLocalBin, mkHomeFiles, systemdUser, ...) |
| `modules/`     | Dendritic module tree (auto-importing domains)                 |
| `packages/`    | Local packages + overlays                                      |
| `scripts/`     | Dev tooling (syntax checks, lint helpers)                      |
| `secrets/`     | sops-nix encrypted secrets                                     |
| `AGENTS.md`    | Agent instructions / repo conventions                          |
| `Justfile`     | Task runner (`just <task>`)                                    |
| `OPTIONS.md`   | features.\* option reference                                   |
| `README.md`    | Repo readme                                                    |
| `flake.nix`    | Flake entry point                                              |
| `treefmt.toml` | Formatting configuration                                       |

## Hosts

### odin

- profiles: desktop, dev, gaming
- `features.apps.winapps.enable = true`
- `features.cli.broot.enable = true`
- `features.dev.ai.omp.enable = true` — Oh My Pi (omp) — AI coding agent fork with LSP, DAP,
  subagents
- `features.gui.vicinae.manageConfig = true` — Nix-managed vicinae theme/settings (neg.nvim-style)
- `features.hardware.usbAutomount.enable = true`
- `features.input.kanata.enable = true` — Caps→Ctrl via kanata
- `features.input.ruHotkeys.enable = true` — us layout in kitty/mpv on focus (RU hotkey fix)
- `features.llm.enable = true` — Local LLM stack: Ollama (ROCm, models on /zero/ai/ollama), colibri
  engine, voxinput
- `features.mail.mbsync.enable = false`
- `features.mail.vdirsyncer.enable = false` — Disabled: no Google OAuth credentials (missing
  secrets/home/vdirsyncer/google.sops.yaml)
- `features.net.ceno.enable = true` — Ceno/Ouinet P2P client (censorship-circumvention node)
- `features.net.netHealth.enable = true` — Periodic net/DNS/zapret2 health check with self-heal +
  ntfy
- `features.net.rknDomains.enable = true` — RKN blocklist -> zapret2 hostlist
- `features.net.zapret2.enable = true` — Zapret2 DPI bypass (nfqws2)
- `features.optimization.enable = true`
- `features.virt.docker.enable = true` — Podman + docker-compat stack (for the Ceno/Ouinet
  container)
- `features.virt.libvirtd.enable = true`
- `features.web.vivaldi.enable = true`

## Module domains (`modules/`)

### appimage

- `default.nix` (auto-import entry)

### apps

- `default.nix` (auto-import entry)
- subdirs: `obsidian/`

### cli

- `default.nix` (auto-import entry)
- files: `dev.nix`, `file-ops.nix`, `media.nix`, `monitoring.nix`, `network.nix`, `ocr.nix`,
  `system.nix`, `text.nix`, `tools.nix`, `ugrep.nix`
- subdirs: `archives/`

### core

- `default.nix` (auto-import entry)
- files: `neg.nix`

### dev

- `default.nix` (auto-import entry)
- files: `ansible.nix`, `omp.nix`
- subdirs: `android/`, `benchmarks/`, `editor/`, `gcc/`, `gdb/`, `git/`, `java/`, `pkgs/`,
  `python/`, `unreal/`

### documentation

- `default.nix` (auto-import entry)

### emulators

- `default.nix` (auto-import entry)
- files: `pkgs.nix`

### features

- `default.nix` (auto-import entry)
- files: `apps.nix`, `cli.nix`, `core.nix`, `dev.nix`, `games.nix`, `gui.nix`, `hardware.nix`,
  `media.nix`, `misc.nix`, `network.nix`, `optimization.nix`, `system.nix`, `web.nix`

### flatpak

- `default.nix` (auto-import entry)
- files: `pkgs.nix`

### fonts

- `default.nix` (auto-import entry)

### fun

- `default.nix` (auto-import entry)
- files: `launchers-packages.nix`, `misc-packages.nix`

### games

- `default.nix` (auto-import entry)
- files: `controllers.nix`

### hardware

- `default.nix` (auto-import entry)
- files: `amdgpu.nix`, `config.nix`, `cooling.nix`, `gpu-corectrl.nix`, `pkgs.nix`, `uinput.nix`,
  `usb-automount.nix`
- subdirs: `audio/`, `cpu/`, `input/`, `io/`, `qmk/`, `udev-rules/`, `video/`, `webcam/`

### llm

- `default.nix` (auto-import entry)
- files: `colibri.nix`, `llama-server.nix`, `ollama.nix`, `open-webui.nix`, `pkgs.nix`

### media

- `default.nix` (auto-import entry)
- files: `ai-upscale-packages.nix`, `multimedia-packages.nix`, `vapoursynth-packages.nix`
- subdirs: `audio/`, `images/`, `scripts/`

### monitoring

- `default.nix` (auto-import entry)
- subdirs: `alertmanager/`, `pkgs/`, `sysstat/`, `vnstat/`

### nix

- `default.nix` (auto-import entry)
- files: `clblast.nix`, `multimon-ng.nix`, `settings.nix`, `wb32-dfu-updater.nix`

### profiles

- `default.nix` (auto-import entry)
- files: `aliases.nix`, `debug.nix`, `desktop.nix`, `dev.nix`, `gaming.nix`, `performance.nix`,
  `security.nix`, `services.nix`, `vm.nix`, `work.nix`

### secrets

- `default.nix` (auto-import entry)
- files: `pkgs.nix`
- subdirs: `pass/`, `yubikey/`

### security

- `default.nix` (auto-import entry)

### servers

- `default.nix` (auto-import entry)
- subdirs: `adguardhome/`, `avahi/`, `geoclue/`, `mpd/`, `openssh/`, `samba/`, `unbound/`

### shell

- `default.nix` (auto-import entry)
- files: `pkgs.nix`, `zsh.nix`

### system

- `default.nix` (auto-import entry)
- files: `boot.nix`, `ceno-client.nix`, `deduplicate-shadow.nix`, `disabled-modules.nix`,
  `environment.nix`, `filesystems.nix`, `irqbalance.nix`, `log-ttys.nix`, `oomd.nix`, `pkgs.nix`,
  `preserve-flake.nix`, `scx.nix`, `swapfile.nix`, `tailscale.nix`, `users.nix`, `virt.nix`,
  `winapps.nix`, `zram.nix`
- subdirs: `boot/`, `kernel/`, `net/`, `systemd/`, `vm/`

### text

- `default.nix` (auto-import entry)
- files: `manipulate-packages.nix`, `notes-packages.nix`, `read-packages.nix`

### tools

- `default.nix` (auto-import entry)
- files: `pkgs.nix`

### torrent

- `default.nix` (auto-import entry)

### user

- `default.nix` (auto-import entry)
- files: `dbus.nix`, `fonts.nix`, `gui-packages.nix`, `locale-pkgs.nix`, `locale.nix`, `locate.nix`,
  `mail.nix`, `neovim.nix`, `theme-packages.nix`, `xdg.nix`
- subdirs: `games/`, `nix-maid/`, `session/`

> User-level config lives under `modules/user/nix-maid/` (`apps/`, `cli/`, `gui/`, `web/`, `sys/`,
> `fun/`). See `modules/README.md` for the human-written domain reference.

## Feature flags (`options.features.*`)

Defined in `modules/features/*.nix` (auto-imported); human-readable reference: `OPTIONS.md`.

### apps

- `features.apps.obsidian.enable` — default: `false` — enable Obsidian knowledge base app + vault
- `features.apps.throne.enable` — default: `false` — enable Throne GUI proxy configuration manager
- `features.apps.winapps.desktopApps` — default: `[ ]` (type: `lib.types.listOf`) — WinApps to
  generate .desktop files for (e.g. [ "excel" "word" "vscode" ])
- `features.apps.winapps.enable` — default: `false` — enable WinApps integration (KVM/libvirt
  Windows VM, RDP bridge)

### cli

- `features.cli.broot.enable` — default: `false` — enable broot file manager and shell integration
- `features.cli.yazi.enable` — default: `true` — enable yazi terminal file manager

### dev

- `features.dev.ai.enable` — default: `true` — enable AI tools (e.g., LM Studio)
- `features.dev.ai.omp.enable` — default: `false` — install Oh My Pi (omp) AI coding agent (fork of
  Pi)
- `features.dev.bpf.enable` — default: `false` — enable BPF tracing tools (bpftrace, below)
- `features.dev.cpp.enable` — default: `true` — enable C/C++ tooling (gcc/clang, cmake, ninja, lldb)
- `features.dev.enable` — default: `true` — enable Dev stack (toolchains, editors, hack tooling)
- `features.dev.haskell.enable` — default: `true` — enable Haskell tooling (ghc, cabal, stack, HLS)
- `features.dev.java.enable` — default: `false` — enable Java/JVM development tooling (JDK, Maven)
- `features.dev.java.maven` — default: `true` — enable Apache Maven build tool
- `features.dev.pkgs.iac` — default: `true` — enable infrastructure-as-code tooling (Terraform,
  etc.)
- `features.dev.pkgs.joern` — default: `true` — enable Joern code analysis platform
- `features.dev.python.core` — default: `true` — enable core Python development packages
- `features.dev.python.tools` — default: `true` — enable Python tooling (LSP, utilities)
- `features.dev.rust.enable` — default: `true` — enable Rust tooling (rustup, rust-analyzer)
- `features.dev.unreal.enable` — default: `false` — enable Unreal Engine 5 tooling
- `features.dev.unreal.useSteamRun` — default: `true` (type: `bool`) — Wrap Unreal Editor launch via
  steam-run to provide FHS runtime libraries.

### devSpeed

- `features.devSpeed.enable` — default: `false` — enable dev-speed mode (trim heavy features for
  faster eval)

### emulators

- `features.emulators.extra.enable` — default: `false` — enable Extra Emulators (PCSX2, DOSBox,
  etc.)
- `features.emulators.retroarch.enable` — default: `false` — enable RetroArch emulator
- `features.emulators.retroarch.full` — default: `false` — use retroarchFull with extended (unfree)
  cores

### excludePkgs

- `features.excludePkgs` — default: `[ ]` (type: `listOf`) — List of package names (pname) to
  exclude from curated home.packages lists.

### flatpak

- `features.flatpak.builder.enable` — default: `false` — enable flatpak-builder

### fun

- `features.fun.enable` — default: `true` — enable fun extras (art collections, etc.)

### games

- `features.games.dosemu.enable` — default: `true` — enable Dosemu
- `features.games.nethack.enable` — default: `true` — enable Nethack
- `features.games.openmw.enable` — default: `false` — enable OpenMW (Morrowind Engine)
- `features.games.oss.enable` — default: `false` — enable OSS Games (SuperTux, Wesnoth, etc.)
- `features.games.steamProxy.enable` — default: `false` — route Steam traffic through local SOCKS5
  proxy (proxychains LD_PRELOAD)

### gpg

- `features.gpg.enable` — default: `true` — enable GPG and gpg-agent (creates ~/.gnupg)

### gui

- `features.gui.enable` — default: `true` — enable GUI stack (wayland/hyprland, quickshell, etc.)
- `features.gui.gtkTheme` — default: `"neg-gtk"` (type: `enum`) — GTK theme to apply system-wide.
- `features.gui.hdr.enable` — default: `false` — enable HDR support (env vars for DXVK, Gamescope,
  Wine)
- `features.gui.iconTheme` — default: `"kora-pgrey"` (type: `str`) — Icon theme to apply system-wide
  (GTK + Qt).
- `features.gui.qt.enable` — default: `true` — enable Qt integrations for GUI (qt6ct,
  hyprland-qt-\*)
- `features.gui.quickshell.enable` — default: `true` — enable Quickshell (panel) at login
- `features.gui.vicinae.enable` — default: `false` — enable Vicinae (Wayland app runner + window
  switcher)
- `features.gui.vicinae.manageConfig` — default: `false` — let Nix manage vicinae theme/settings
  (disable for interactive config)

### hardware

- `features.hardware.amdgpu.rocm.enable` — default: `false` — enable AMDGPU ROCm support
- `features.hardware.bluetooth.enable` — default: `false` — enable Bluetooth support

### input

- `features.input.kanata.enable` — default: `false` — enable Kanata keyboard remapper (requires
  uinput module)
- `features.input.ruHotkeys.enable` — default: `false` — enable per-window keyboard layout switching
  (us in hotkey-heavy apps)
- `features.input.ruHotkeys.pollSec` — default: `"0.5"` (type: `str`) — Active-window poll interval
  in seconds (fractional values allowed).
- `features.input.ruHotkeys.ruLayoutIndex` — default: `1` (type: `int`) — XKB group index of the ru
  layout (kb_layout = us,ru invariant).
- `features.input.ruHotkeys.usClasses` — default:
  `[ "term" # plain shell (kitty --class term) "nwim" # nvim wrapper (kitty --class nwim) "music" # rmpc (M4+f) "teardown" # btop (M4+d) "torrment" # rustmission (M4+t) "vpn" # tun status (M4+u) "mixer" # ncpamixer (M4+C+p) "rebuild" # nh os switch (M4+S+n) "mpd-add" # rmpc spawns this class "mpv" # video player: no text input, vim-style keys ]`
  (type: `listOf`) — Hyprland window classes forced to the us layout on focus. Everything else
  defaults to ru (typing-first). Switching happens only on focus transitions — a manual M4+S switch
  stays until the next focus change.
- `features.input.ruHotkeys.usLayoutIndex` — default: `0` (type: `int`) — XKB group index of the us
  layout (kb_layout = us,ru invariant).

### llm

- `features.llm.enable` — default: `false` — enable local LLM stack (Ollama, local-ai)

### mail

- `features.mail.enable` — default: `true` — enable Mail stack (notmuch, isync, vdirsyncer, etc.)
- `features.mail.mbsync.enable` — default: `true` — enable mbsync IMAP sync service/timer
- `features.mail.vdirsyncer.enable` — default: `true` — enable Vdirsyncer sync service/timer

### media

- `features.media.aiUpscale.enable` — default: `false` — enable AI upscaling integration for video
  (mpv)
- `features.media.audio.apps.enable` — default: `true` — enable audio apps (players, tools)
- `features.media.audio.beets.enable` — default: `true` — enable Beets music library manager
- `features.media.audio.beets.mode` — default: `"distrobox"` (type: `lib.types.enum`) — Beets
  runtime mode: native (Nixpkgs) or distrobox (CachyOS container)
- `features.media.audio.carlaLoopback.enable` — default: `false` — enable virtual loopback sink for
  Carla
- `features.media.audio.cider.enable` — default: `false` — enable Cider (Apple Music client)
- `features.media.audio.core.enable` — default: `true` — enable audio core (PipeWire routing tools)
- `features.media.audio.creation.enable` — default: `true` — enable audio creation stack (DAW,
  synths)
- `features.media.audio.mpd.enable` — default: `true` — enable MPD stack (mpd, clients, mpdris2)
- `features.media.audio.spicetify.enable` — default: `false` — enable Spicetify (Spotify
  customization)
- `features.media.audio.spotify.enable` — default: `false` — enable Spotify stack (spotifyd daemon,
  spotify-tui)
- `features.media.photo.enable` — default: `false` — enable photography workflow (darktable,
  rawtherapee, testdisk)
- `features.media.webcam.enable` — default: `false` — enable virtual webcam support (v4l2loopback)

### net

- `features.net.bbrv3.enable` — default: `true` — enable TCP BBRv3 congestion control (kernel >=
  6.18)
- `features.net.ceno.enable` — default: `false` — enable Ceno/Ouinet P2P client
  (censorship-circumvention node, podman container)
- `features.net.netHealth.enable` — default: `false` — enable periodic network/DNS/zapret2 health
  check with self-heal and ntfy push
- `features.net.proxy.enable` — default: `false` — enable Xray SOCKS5 proxy (127.0.0.1:10808)
- `features.net.rknDomains.enable` — default: `false` — enable RKN domain blocklist fetcher with
  daily timer
- `features.net.tailscale.enable` — default: `false` — enable Tailscale mesh VPN and Tailray GUI
- `features.net.wifi.enable` — default: `false` — enable Wi-Fi stack and management tools (iwd,
  wavemon, etc.)
- `features.net.zapret2.enable` — default: `false` — enable Zapret2 DPI bypass via nfqueue (requires
  zapret2 package)

### secrets

- `features.secrets.enable` — default: `true` — enable secrets tooling (pass, YubiKey helpers)

### system

- `features.system.logTtys.auth.enable` — default: `true` — AUTH log viewer on tty13 (auth messages)
- `features.system.logTtys.crit.enable` — default: `true` — CRIT log viewer on tty8 (emerg..crit)
- `features.system.logTtys.enable` — default: `true` — Per-TTY log classification (journalctl
  viewers on tty8,tty10-tty16)
- `features.system.logTtys.err.enable` — default: `true` — ERR log viewer on tty10 (errors)
- `features.system.logTtys.full.enable` — default: `true` — FULL log viewer on tty16 (all messages)
- `features.system.logTtys.kernel.enable` — default: `true` — KERNEL log viewer on tty12 (kernel
  messages)
- `features.system.logTtys.network.enable` — default: `true` — NETWORK log viewer on tty15 (network
  daemons)
- `features.system.logTtys.networkUnits` — default:
  `[ "NetworkManager.service" "sshd.service" "nftables.service" ]` (type: `lib.types.listOf`) —
  Systemd units to monitor on tty15 (network TTY). Override per-host.
- `features.system.logTtys.systemd.enable` — default: `true` — SYSTEMD log viewer on tty14 (systemd
  messages)
- `features.system.logTtys.warn.enable` — default: `true` — WARN log viewer on tty11 (warnings)

### text

- `features.text.manipulate.enable` — default: `true` — enable text/markup manipulation CLI tools
  (jq/yq/htmlq)
- `features.text.notes.enable` — default: `true` — enable notes tooling (zk CLI)
- `features.text.read.enable` — default: `true` — enable reading stack (CLI/GUI viewers, OCR,
  Recoll)

### torrent

- `features.torrent.enable` — default: `true` — enable Torrent stack (Transmission, tools, services)

### virt

- `features.virt.docker.enable` — default: `false` — enable docker/podman virtualization
- `features.virt.libvirtd.enable` — default: `false` — enable libvirtd (KVM/QEMU) virtualization

### web

- `features.web.chat.enable` — default: `true` — enable Telegram chat client (static binary,
  GTK-free)
- `features.web.default` — default: `null` (type: `str`) — Default browser used for XDG handlers,
  $BROWSER, and integrations.
- `features.web.enable` — default: `true` — enable Web stack (browsers + tools)
- `features.web.tools.enable` — default: `true` — enable web tools (aria2, yt-dlp, misc)
- `features.web.vivaldi.enable` — default: `false` — enable Vivaldi browser

## Profiles (`modules/profiles/`)

Each profile applies `mkDefault` overrides when its name is in `features.profiles` (host sets it;
later profiles win).

### desktop

- `features.gui.enable` → `true` (mkDefault)
- `features.web.enable` → `true` (mkDefault)
- `features.mail.enable` → `true` (mkDefault)
- `features.dev.enable` → `true` (mkDefault)
- `features.fun.enable` → `true` (mkDefault)
- `features.torrent.enable` → `true` (mkDefault)
- `features.media.audio.core.enable` → `true` (mkDefault)
- `features.media.audio.apps.enable` → `true` (mkDefault)
- `features.media.audio.creation.enable` → `true` (mkDefault)
- `features.media.audio.mpd.enable` → `true` (mkDefault)
- `features.emulators.retroarch.full` → `true` (mkDefault)
- `features.dev.ai.enable` → `true` (mkDefault)
- `features.gui.vicinae.enable` → `true` (mkDefault)

### dev

- `features.dev.enable` → `true` (mkDefault)
- `features.dev.ai.enable` → `true` (mkDefault)
- `features.dev.rust.enable` → `true` (mkDefault)
- `features.dev.cpp.enable` → `true` (mkDefault)
- `features.dev.haskell.enable` → `true` (mkDefault)
- `features.dev.java.enable` → `true` (mkDefault) # enable Java/JVM toolchain (JDK, Maven,
  PraxisLIVE)
- `features.dev.pkgs.iac` → `true` (mkDefault) # enable Infrastructure as Code tools (terraform,
  opentofu)

### gaming

- `features.optimization.enable` → `true` (mkDefault)
- `features.gui.hdr.enable` → `true` (mkDefault)

## Local packages (`packages/`)

| Package dir            | Package                                     | Description                                                                                                    |
| ---------------------- | ------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| `albumdetails/`        | `albumdetails` (0.1)                        | Generate details for music album                                                                               |
| `andromeda-gtk-theme/` | `andromeda-gtk-theme` (unstable-2026-05-19) | Andromeda — an elegant dark GTK theme for Gnome, MATE, Budgie, Cinnamon, XFCE                                  |
| `brrtfetch/`           | `brrtfetch` (0-unstable-2026-01-10)         | Render animated ASCII art from a GIF for your sysinfo fetcher of choice                                        |
| `camillagui/`          | `camillagui` (4.1.0)                        | Web GUI for CamillaDSP (aiohttp backend + prebuilt React frontend)                                             |
| `dirt-samples/`        | `dirt-samples` (unstable-2025-07-21)        | Audio sample library for SuperDirt / TidalCycles live coding                                                   |
| `dsh/`                 | `dsh` (0.1.0-rc.6)                          | DeepSeek Harness (dsh) — open-source agent harness, everything is a plugin                                     |
| `duf/`                 | `duf` (0.9.1-neg)                           | Disk Usage/Free Utility (fork with plain style support)                                                        |
| `flight-gtk-theme/`    | `flight-gtk-theme` (unstable-2025-06-13)    | Flight GTK theme (dark and light variants)                                                                     |
| `game/`                | `game` (0.1.0)                              | Unified game launcher — CPU pinning, Gamescope presets, session launching                                      |
| `genlc-rs/`            | `genlc` (0.2.0)                             | Genelec SAM loudspeaker CLI volume control (Rust)                                                              |
| `ghgrab/`              | `ghgrab` (2.0.1)                            | Search and download files from GitHub without leaving your CLI                                                 |
| `hdspeconf/`           | `hdspeconf` (unstable-2022-03-30)           | User space configuration tool for RME HDSPe cards (snd-hdspe driver)                                           |
| `hwctl/`               | `hwctl` (0.1.0)                             | Hardware control CLI — CPU boost toggle, V-Cache mask recommendations, Nuvoton fan control                     |
| `hypr-focus/`          | `hypr-focus` (0.1.0)                        | Hyprland focus history tracker and window management CLI                                                       |
| `hyprwhspr/`           | `vicinae-hyprwhspr` (0.1.0)                 | Vicinae extension for hyprwhspr dictation control                                                              |
| `joern/`               | `joern` (4.0.599)                           | Open-source code analysis platform for C/C++, Java, JS, Python, and more                                       |
| `lazytail/`            | `lazytail` (0.10.0)                         | A fast, universal terminal-based log viewer with live filtering and follow mode                                |
| `matugen-themes/`      | `matugen-themes` (unstable-2025-11-01)      | Template collection for Matugen theme generator                                                                |
| `omp/`                 | `omp` (17.3.4)                              | Oh My Pi (omp) — AI coding agent with LSP, DAP, subagents, and more                                            |
| `oryx/`                | `oryx` (0.8.0)                              | TUI for sniffing network traffic using eBPF                                                                    |
| `pretty-printer/`      | `neg-pretty-printer` (0.1.0)                | Custom pretty-printer utilities (colors + file info)                                                           |
| `pw-audioshare/`       | `pw-audioshare` (1.0.6)                     | Accessible GTK4 PipeWire patchbay with auto-connect presets                                                    |
| `pwroute/`             | `pwroute` (0.1.0)                           | PipeWire audio router for RME HDSPe AIO Pro                                                                    |
| `pycamilladsp/`        | `pycamilladsp` (4.0.0)                      | Python library for communicating with CamillaDSP                                                               |
| `pycamilladsp-plot/`   | `pycamilladsp-plot` (4.1.0)                 | Validate, evaluate and plot configs and filters for CamillaDSP                                                 |
| `reddix/`              | `reddix` (0.2.9)                            | Reddit, refined for the terminal                                                                               |
| `repeater/`            | `repeater` (0.1.10)                         | Spaced repetition, in your terminal                                                                            |
| `resterm/`             | `resterm` (0.41.1)                          | Terminal REST client for .http/.rest files with HTTP, GraphQL and gRPC support                                 |
| `simutil/`             | `simutil` (0.5.0)                           | Cross platform utility TUI app for launching iOS simulators / Android emulators                                |
| `snd-hdspe/`           | `snd-hdspe` (unstable-2022-03-30)           | New Linux ALSA driver for RME HDSPe MADI/AES/RayDAT/AIO/AIO Pro                                                |
| `strace-tui/`          | `strace-tui` (1.0.1)                        | TUI for visualizing and exploring strace output                                                                |
| `superdirt/`           | `superdirt` (1.7.4-unstable-2025-07-21)     | SuperCollider quark providing the audio engine for TidalCycles live coding                                     |
| `swayimg/`             | `swayimg` (0-unstable-fork)                 | Image viewer for Wayland (forked from artemsen/swayimg, Vulkan-accelerated)                                    |
| `talktype/`            | `talktype`                                  | Push-to-talk voice typing that works everywhere                                                                |
| `termeverything/`      | `termeverything` (0.7.8)                    | Run GUI windows inside your terminal — a Wayland compositor that renders to the terminal via ANSI escape codes |
| `tmd-top/`             | `typing-extensions` (4.9.0)                 | Real-time Linux network traffic monitor with per-IP connection and bandwidth breakdown (TUI)                   |
| `ttf-code2000/`        | `ttf-code2000` (1.176)                      | Code2000 shareware Unicode TrueType font with broad script coverage                                            |
| `ttf-code2001/`        | `ttf-code2001` (0.922)                      | Code2001 freeware Unicode TrueType font for Plane 1 (ancient/historic scripts)                                 |
| `ttf-code2002/`        | `ttf-code2002` (0.922)                      | Code2002 freeware Unicode TrueType font for Plane 2 (rare CJK ideographs)                                      |
| `ttf-code20x3/`        | `ttf-code20x3` (0.904)                      | Code20X3 freeware Unicode TrueType font for Plane 3 (CJK Extensions G/H)                                       |
| `v2raya/`              | `v2raya` (2.2.7.5)                          | A web GUI client of Project V                                                                                  |
| `vowel/`               | `vowel` (unstable-2025-07-25)               | SuperCollider quark with formant tables (Vowel class), used by SuperDirt                                       |
| `wallhaven/`           | `vicinae-wallhaven` (0.1.0)                 | Vicinae extension for browsing wallhaven.cc wallpapers                                                         |
| `watchtower/`          | `watchtower` (1.0.0)                        | A clean, minimal, terminal-based global intelligence dashboard                                                 |
| `waves/`               | `waves` (0.1.46)                            | Keyboard-driven terminal music player with Soulseek downloads and Last.fm integration                          |
| `witr/`                | `witr` (0.3.2)                              | A Linux CLI tool that explains the causal chain behind running processes                                       |
| `wl/`                  | `wl` (0.1.0)                                | Vulkan-accelerated wallpaper daemon for Wayland compositors                                                    |
| `wl-switcher/`         | `vicinae-wl-switcher` (0.1.0)               | Vicinae extension for wl wallpaper daemon — browse and switch wallpapers                                       |
| `zapret2/`             | `zapret2` (unstable-2026-08-03)             | DPI bypass tool — nfqueue-based traffic filter (nfqws2)                                                        |
| `zsh-native-syntax/`   | `zsh-native-syntax` (0.1.0)                 | Native Rust-powered syntax highlighting engine for Zsh (zle module)                                            |

### Overlay wiring (`packages/overlays/`)

| Attr                  | Source                |
| --------------------- | --------------------- |
| `albumdetails`        | `albumdetails`        |
| `andromeda-gtk-theme` | `andromeda-gtk-theme` |
| `brrtfetch`           | `brrtfetch`           |
| `camillagui`          | `camillagui`          |
| `duf`                 | `duf`                 |
| `flight-gtk-theme`    | `flight-gtk-theme`    |
| `genlc`               | `genlc-rs`            |
| `ghgrab`              | `ghgrab`              |
| `hdspeconf`           | `hdspeconf`           |
| `hwctl`               | `hwctl`               |
| `hypr-focus`          | `hypr-focus`          |
| `hyprwhspr`           | `hyprwhspr`           |
| `lazytail`            | `lazytail`            |
| `matugen-themes`      | `matugen-themes`      |
| `omp`                 | `omp`                 |
| `oryx`                | `oryx`                |
| `pretty_printer`      | `pretty-printer`      |
| `pw-audioshare`       | `pw-audioshare`       |
| `pwroute`             | `pwroute`             |
| `pycamilladsp`        | `pycamilladsp`        |
| `pycamilladsp-plot`   | `pycamilladsp-plot`   |
| `reddix`              | `reddix`              |
| `repeater`            | `repeater`            |
| `resterm`             | `resterm`             |
| `simutil`             | `simutil`             |
| `snd-hdspe`           | `snd-hdspe`           |
| `strace-tui`          | `strace-tui`          |
| `swayimg`             | `swayimg`             |
| `talktype`            | `talktype`            |
| `termeverything`      | `termeverything`      |
| `v2raya`              | `v2raya`              |
| `wallhaven`           | `wallhaven`           |
| `watchtower`          | `watchtower`          |
| `waves`               | `waves`               |
| `witr`                | `witr`                |
| `wl`                  | `wl`                  |
| `wl-switcher`         | `wl-switcher`         |
| `zapret2`             | `zapret2`             |
| `zsh-native-syntax`   | `zsh-native-syntax`   |

## Pointers

- `modules/README.md` — human-written module documentation
- `docs/index.md` — documentation hub (manual, howtos, runbooks)
- `docs/howto/agent-recipes.md` — verified change workflows (module, flag, package, host, script,
  secret, commit)
- `OPTIONS.md` — features.\* options reference
- `Justfile` — dev tasks (`just <task>`)
