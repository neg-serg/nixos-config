// dsh-gui-tweaks host half — pure browser-side capability. The empty apply
// exists so the row appears in the composition / loader (the client half
// ships through package.json exports["./client"], like dsh-terminal-ui).
function apply() {}

export { apply };
