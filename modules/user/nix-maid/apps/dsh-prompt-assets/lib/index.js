// dsh-prompt host half — pure browser-side capability. The empty apply
// exists so the row appears in the composition / loader (the client half
// ships through package.json exports["./client"], like dsh-gui-tweaks).
function apply() {}

export { apply };
