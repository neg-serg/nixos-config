# Repo audit — 2026-09-15

Procedure: `docs/runbook-audit.md` (4 zones in parallel subagents, verify, triage A/B/C/D). Previous
full run: `audit-2026-07-21.md` (99 findings, 18 critical) — archived with the old agent workspace,
not in the repo.

Home for audit reports: `.agent/audit/` (tracked). The runbook used to point at a gitignored
"private agent workspace" that does not exist; the first copy of this report was written to `~/tmp/`
and was deleted by an unrelated cleanup the same day.

## Snapshot

- commit at audit start: `0797e878c` (`[web/vivaldi] Close the tab with Ctrl+G`)
- system generation: `system-1836-link`
- audit start: 2026-09-15 11:15 MSK
- working tree: clean, `main` == `origin/main`

## Context that is NOT a finding

- git history rewritten with `git filter-repo` on 2026-08-25 (refs/original, .git/filter-repo
  metadata are expected leftovers);
- 2026-09-15: the licensed Renoise/Redux tarballs (>100 MB) moved out of the repo to
  `/home/neg/nixos-src/renoise` and wired as the `renoise-src` path input (GitHub rejects files
  > 100 MB; untracked/ignored paths never reach the flake source, absolute paths are rejected in
  > pure evaluation);
- 2026-09-15: `files/media/pipewire/pipewire.conf.d/99-tidal-quantum.conf` deleted on purpose (it
  never took effect — `clock-rate.conf` sorts after it);
- odin's restricted domain set (`odinDomains` excludes `appimage`/`apps`) is intentional.

## Triaged themes

Buckets: **A** bugs · **B** hygiene · **C** structure · **D** process. Status: `fixed <commit>` or
`open`.

| #   | Bucket | Theme                                                                                                                                                                                                                                                                                                                                                                                                                             | Sev    | Status                           |
| --- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | -------------------------------- |
| A1  | A      | `features.cli.enable` undeclared → fastfetch-blizzard module dead                                                                                                                                                                                                                                                                                                                                                                 | high   | fixed `5583d8c09`                |
| A2  | A      | `features.excludePkgs` inert (fills ghc/rustup/gcc/jdk; only consumer filters 3 xdg names; cites nonexistent `lib.neg.pkgsList`)                                                                                                                                                                                                                                                                                                  | high   | fixed `c4a45dc92`                |
| A3  | A      | `features.web.default`: `types.str` + `default = null`                                                                                                                                                                                                                                                                                                                                                                            | medium | fixed `a6745936e`                |
| A4  | A      | `docs/runbook-index.md`: all 6 runbook links broken                                                                                                                                                                                                                                                                                                                                                                               | high   | fixed `7aa4de2ea`                |
| A5  | A      | `gen-codebase` regex: comments borrowed from the next line, commented-out flags reported as enabled                                                                                                                                                                                                                                                                                                                               | medium | fixed `fa2adec4b`                |
| A6  | A      | Checks never forced `config`; `mkStubArgs` drifted from `mkSpecialArgs`                                                                                                                                                                                                                                                                                                                                                           | medium | fixed `2bfe46dfb`                |
| A7  | A      | A/B test hosts used `allDomains` instead of `odinDomains`                                                                                                                                                                                                                                                                                                                                                                         | medium | fixed `2dceb7ba8`                |
| B1  | B      | README contradicted reality (all numbers re-measured, CI claims relabelled as local gates, anchors fixed): "403 Nix files" (502), "28 domains" (27), "14 features" (16), "65 packages" in `modules/cli/tools.nix` (28), "7 scratchpads" (:52) vs "6 Scratchpads" (:211) vs 8 rules in `hyprland.lua`, "Version 26.05"/"July 2026" (:16,:609) vs 26.11 / HEAD, a "CI/CD Pipeline" section (:385-412) with no CI config in the repo | medium | open                             |
| B2  | B      | `.ignore:3,8` hides tracked files from rg/fd (`files/nvim/lazy-lock.json`, `files/shell/zsh/lazyfuncs/_lutgen`) — also the generated ripgreprc's `--glob=!package-lock.json`; both fixed                                                                                                                                                                                                                                          | medium | open                             |
| B3  | B      | `docs/howto/index.md` mangled by the RU→EN translation — repaired from the pre-translation file                                                                                                                                                                                                                                                                                                                                   | medium | open                             |
| B4  | B      | Broken paths/links (5 dead markdown links + stale backtick paths): `docs/README.md:59,702` (`docs/runbooks/`), `docs/README.md:118` (`modules/lib/systemd-user.nix`), `secrets/README.md:7-8`, `secrets/home/AGENTS.md:10` (`secrets/home/default.nix`), `docs/runbook-scripts.md:15,19,21`, missing `docs/howto/dsh-web-forks.md`                                                                                                | medium | open                             |
| B5  | B      | Justfile references dead modules: `modules/dev/cachix/*` (:74,78), `modules/user/mail/isync/*` (:124), `sys/dsh-web-ui-build.nix` (:299-309, deleted in 4a31f6de5)                                                                                                                                                                                                                                                                | medium | open                             |
| B6  | B      | `treefmt.toml:17` comment says `files/obsidian-vault/**` is "not repo content" while 44 files there are tracked                                                                                                                                                                                                                                                                                                                   | low    | open                             |
| B7  | B      | `meta` gaps: `packages/renoise-osc`, `packages/wineapps`, `packages/torch-rocm` without `meta`; 94 of 104 packages leave `maintainers = [ ]`                                                                                                                                                                                                                                                                                      | low    | open                             |
| B8  | B      | `local-bin`: 5 scripts without the exec bit in git; `need()` duplicated in 11 scripts, `usage()` in 9                                                                                                                                                                                                                                                                                                                             | low    | open                             |
| B9  | B      | `packages/overlays/disable-checks.nix`: no-op `pythonPackagesExtensions` block, self-flagged "AUDIT: try removing" entries                                                                                                                                                                                                                                                                                                        | low    | open                             |
| B10 | B      | Docs hygiene: 5 docs with no inbound links, `agent-memory-pipeline.md` duplicated by `designs/memory-pipeline.md`, 24-line `runbook-scripts.md` stub, README anchor drift                                                                                                                                                                                                                                                         | low    | open                             |
| B11 | B      | `scripts/dev/test-ollama-chat.sh` and `check-dsh-sessions.sh` invoked by nothing; `check-osh-syntax.sh` without exec bit                                                                                                                                                                                                                                                                                                          | low    | open                             |
| C1  | C      | Prune unused inputs (`hy3` → 17 dead lock nodes, `wrappers`, `agent-skills`) — done; `nixpkgs.follows` for extra-container/raise/tailray/lusty: DECIDED to keep the pins — follows would rebuild raise/tailray against our nixpkgs for no functional gain                                                                                                                                                                         | medium | fixed `88dedeb94` (pruning only) |
| C2  | C      | `allDomains`/`odinDomains` duplicate the same tail → derive by filter                                                                                                                                                                                                                                                                                                                                                             | medium | fixed `9a125340c`                |
| C3  | C      | Overlay order: `neg-pkgs` applied after `packages/overlay.nix` overrides `pkgs.neg.*`, `python3-lto`, `buildFHSEnv` — DECIDED to keep the order (intentional per the python3-lto comment) and document it, which packages/overlays/README.md now does                                                                                                                                                                             | medium | open (decision)                  |
| C4  | C      | `packages/overlays/functions.nix:24-82`: 7 helpers with zero references (documented in the overlay README)                                                                                                                                                                                                                                                                                                                        | medium | fixed `448dded1b`                |
| C5  | C      | Duplicated gates: quickshell ×3, wifi ×4                                                                                                                                                                                                                                                                                                                                                                                          | medium | fixed `8a32d8f01`                |
| C6  | C      | 22 hand-rolled systemd-user units: NOT a mechanical migration — `lib/systemd-user.nix` emits raw `Unit`/`Service`/`Install` (mkSimpleService/mkSimpleTimer/mkSimpleSocket are unused), while the units use the module shape (`serviceConfig`/`wantedBy`/`description`). Needs a module-shaped helper first                                                                                                                        | medium | open                             |
| C7  | C      | Four near-identical `vivaldi-*-pref` units (`modules/user/nix-maid/web/vivaldi.nix:183-264`) — folded into one `vivaldi-prefs` oneshot                                                                                                                                                                                                                                                                                            | low    | fixed `b3bd6efdb`                |
| C8  | C      | PipeWire quantum declared twice (`modules/hardware/audio/pipewire.nix:43` vs `files/media/pipewire/pipewire.conf.d/clock-rate.conf:26`) — the module now keeps only non-clock keys                                                                                                                                                                                                                                                | low    | fixed `8af98f622`                |
| C9  | C      | Dead modules/stubs (deleted: profiles/aliases.nix, hardware/cpu/default.nix, hostExtras, 3 unreachable assertions): `modules/profiles/aliases.nix`, `modules/hardware/cpu/default.nix`, unreachable assertions `modules/features/default.nix:116-129`, `hostExtras` never fires                                                                                                                                                   | low    | open                             |
| C10 | C      | Flags without consumers (features.dev.haskell deleted; apps.obsidian documented as apps-domain-only): `features.dev.rust`/`haskell` (only via the removed excludePkgs), `features.apps.obsidian` (`apps` domain off on odin)                                                                                                                                                                                                      | medium | open                             |
| C11 | C      | Doc drift (modules/README `lib` row removed; the docs generator now really covers every features.\* flag): `modules/README.md:34` documents a nonexistent `lib` domain; `OPTIONS.md:7-8` overclaims `docs/howto/modules.md` coverage                                                                                                                                                                                              | medium | open                             |
| C12 | C      | One floating rev left (pinned to a210f8b5, hash matches): `packages/glm-osc/default.nix:33` `rev = "master"`                                                                                                                                                                                                                                                                                                                      | medium | open                             |
| D1  | D      | Audit reports had no home (runbook pointed at a nonexistent gitignored workspace)                                                                                                                                                                                                                                                                                                                                                 | low    | fixed here (`.agent/audit/`)     |
| D2  | D      | README promises CI that does not exist (gates are local: `.githooks/pre-commit` → `just lint`)                                                                                                                                                                                                                                                                                                                                    | medium | open                             |
| D3  | D      | No freshness gate for generated docs (`codebase.md`, `docs-modules.md`)                                                                                                                                                                                                                                                                                                                                                           | medium | open                             |
| D4  | D      | `neg-pkgs` (nixos-pkgs) subtree diverged after the history rewrite — DECIDED to leave it: the split is guaranteed non-fast-forward, so syncing needs a force-push that rewrites that repo's public history (run `just subtree-push-packages` only after an explicit decision)                                                                                                                                                     | medium | open                             |
| D5  | D      | Manual counts/versions in README → no regeneration step in the process                                                                                                                                                                                                                                                                                                                                                            | low    | open                             |

## Fixed so far (A1–A7, C1-pruning, C2, C4, C5, C7–C12, B1–B4, D1 — details in the commit messages)

- **A1** `5583d8c09` — the gate in `modules/user/nix-maid/cli/fastfetch.nix:11` was broken since the
  module was created (`a9b6199a8`); `features.cli.enable` never existed. Flag added (default true);
  `enabled "cli"` is true and the fastfetch home files are managed again.
- **A2** `c4a45dc92` — option, builder and filter deleted; the one ungated spot (`pkgs.rustup`,
  `pkgs.rust-analyzer` in `hosts/odin/services/policy.nix`) now follows `features.dev.rust.enable`;
  OPTIONS.md/docs refreshed, generated docs regenerated.
- **A3** `a6745936e` — `types.nullOr types.str`.
- **A4** `7aa4de2ea` — six `runbook-` prefixes.
- **A5** `fa2adec4b` — anchored regex; `tpmSudo` no longer reported as enabled; comments stay on
  their own line.
- **A6** `2bfe46dfb` — checks build their args from `mkSpecialArgs`; the test-host check forces the
  full system evaluation with `builtins.seq` (`drvPath` would have pulled the whole closure into the
  check's build plan — measured, not guessed).
- **A7** `2dceb7ba8` — `mkTestHost` uses the host's domain filter; `test-odin-gaming` fails if the
  `appimage` domain is imported.

## Findings raised during the fixes

- **N1 (medium) · forcing the whole user-unit set fails** —
  `nix eval .#nixosConfigurations.odin.config.systemd.user.services` aborts with
  `The option 'systemd.user.services.adw-bluetooth-daemon.startLimitBurst' was accessed but has no value defined`.
  The switch itself is fine; only whole-set inspection/JSON dumps break, which also blocks the "dump
  units before/after a refactor" verification technique.
- **N2 (medium) · the generated global ripgrep config hid tracked files** — the
  `--glob=!package-lock.json` line in `modules/user/nix-maid/cli/search.nix` (rendered to
  `~/.config/ripgrep/ripgreprc`) hides the three tracked `package-lock.json` files from rg/fd; fixed
  together with B2.
- **N3 (low) · dead helper surface** — `mkSimpleService`, `mkSimpleTimer` and `mkSimpleSocket` in
  `lib/systemd-user.nix` have no call sites; only `mkUserOneshot` and `mkUnitFromPresets` are used.
  Decide with C6: either add module-shaped variants or drop the unused three.

## Notes for the next run

- Batch A (A1–A7), D1, C1 (pruning), C2, C4 and C5 are done (see the status column and the commit
  messages). Still open: **C3** (decision), **C6** (22 hand-rolled systemd-user units — largest),
  **C7–C12**, the whole **B** block, **D2–D5**, and the `nixpkgs.follows` half of C1 (raises/changes
  derivations → rebuild budget needed).
- Cheap next steps: C7 (fold the four `vivaldi-*-pref` units into one) and C8 (single source for the
  PipeWire quantum) are small and self-contained; B2/B3 (`.ignore`, `docs/howto/index.md`) are
  one-file fixes.
- Re-verify before acting: subagent findings were spot-checked by the orchestrator where they were
  picked up; the rest must be confirmed file-by-file when they are taken (runbook §3).
