#!/usr/bin/env bash
set -euo pipefail

# Run the full lint suite (Markdown language policy, QML, Hyprland variables,
# all additional syntax checks) inside the dedicated `lint` devshell, which
# provides every tool without the pre-commit shellHook of the default shell.
nix develop .#lint --command just lint
