#!/usr/bin/env bash
set -euo pipefail

# Run the full lint suite (includes Markdown language policy, QML, Hyprland
# variables, and all additional syntax checks). Runs inside `nix develop`,
# which provides just and every lint tool via the devshell.
just lint
