# iris: shell auto-completion (IntelliSense-style suggestions).
# DISABLED (2026-08-27): autosuggestions removed; Tab belongs to fzf-on-tab
# (04-fzf.zsh) — completion + ~/m/new fuzzy expansion — and Ctrl+R belongs to
# zhist (05-zhist.zsh). To re-enable iris: uncomment the line below AND set
# toggle-mode = "ctrl+g" in ~/.config/iris/config.toml so Ctrl+R stays zhist's.
# (( $+commands[iris] )) && eval "$(iris init zsh)"
