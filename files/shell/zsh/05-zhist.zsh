# zhist: smarter history store (dir, exit status, duration) + fzf picker.
# PARKED (2026-08-27): hook disabled — decide later what to do with zhist.
# Without it, Ctrl+R falls back to fzf's history picker (key-bindings.zsh).
# To re-enable, uncomment:
# (( $+commands[zhist] )) && eval "$(zhist init -no-arrow-binds)"
