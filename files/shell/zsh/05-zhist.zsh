# zhist: smarter history store (dir, exit status, duration) + fzf picker.
# DISABLED (2026-08-27): iris takes over suggestions/history — Ctrl+R is
# iris history mode (see files/iris/config.toml). To re-enable zhist, uncomment:
# (( $+commands[zhist] )) && eval "$(zhist init -no-arrow-binds)"
