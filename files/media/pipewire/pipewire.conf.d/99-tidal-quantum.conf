# TidalCycles / SuperCollider low-latency quantum
# Takes effect on pipewire restart: systemctl --user restart pipewire
# Verify: pw-metadata -n settings clock.quantum
context.properties = {
    default.clock.quantum    = 128
    default.clock.min-quantum = 64
    default.clock.max-quantum = 2048
}
