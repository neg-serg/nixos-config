context.modules = [
    {   name = libpipewire-module-loopback
        args = {
            capture.props = {
                media.class = Audio/Sink
                audio.position = [ FL FR ]
                node.name = pw-loopback
                node.description = "carla-loopback"
                channelmix.mute = true
                channelmix.volume = 0.0
            }
            playback.props = {
                node.name = "playback.pw-loopback"
                node.passive = true
                audio.position = [ FL FR ]
                stream.dont-remix = true
                # target.object = "alsa_output.pci-...hdsp...analog-stereo"  # set to your HDSPe sink
                target.object = "Realphones"
            }
        }
    }
]
