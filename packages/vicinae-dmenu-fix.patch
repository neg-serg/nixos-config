diff --git a/src/lib/figura/src/codegen/glaze-qt.hpp b/src/lib/figura/src/codegen/glaze-qt.hpp
--- a/src/lib/figura/src/codegen/glaze-qt.hpp
+++ b/src/lib/figura/src/codegen/glaze-qt.hpp
@@ -200,9 +200,13 @@
   }
 
 private:
   AbstractLogger* m_logger = nullptr;
-  int m_id = 1;
+  // Each client process must use ids that cannot collide with another
+  // concurrent client: the IPC server routes asynchronous replies by id
+  // alone, and a reused id makes it deliver a reply to the wrong peer
+  // or drop it entirely. See vicinaehq/vicinae#1827.
+  int m_id = static_cast<int>(Clock::now().time_since_epoch().count() & 0x7fffffff) | 1;
   std::unordered_map<std::string, std::vector<ResponseHandler>> m_handlers;
   std::unordered_map<int, PendingRequest> m_requestMap;
   AbstractTransport &m_transport;
   std::string m_buf;
diff --git a/src/lib/figura/src/codegen/glaze.hpp b/src/lib/figura/src/codegen/glaze.hpp
--- a/src/lib/figura/src/codegen/glaze.hpp
+++ b/src/lib/figura/src/codegen/glaze.hpp
@@ -147,9 +147,13 @@
     return {};
   }
 
 private:
-  int m_id = 1;
+  // Each client process must use ids that cannot collide with another
+  // concurrent client: the IPC server routes asynchronous replies by id
+  // alone, and a reused id makes it deliver a reply to the wrong peer
+  // or drop it entirely. See vicinaehq/vicinae#1827.
+  int m_id = static_cast<int>(std::chrono::steady_clock::now().time_since_epoch().count() & 0x7fffffff) | 1;
   std::unordered_map<std::string, std::vector<ResponseHandler>> m_handlers;
   std::unordered_map<int, ResponseHandler> m_requestMap;
   AbstractTransport &m_transport;
   std::string m_buf;
@@ -433,8 +437,9 @@
     std::ostringstream oss;
 
     oss << R"(
 #pragma once
+#include <chrono>
 #include <expected>
 #include <functional>
 #include <glaze/glaze.hpp>
 #include <string>
diff --git a/src/server/src/navigation-controller.cpp b/src/server/src/navigation-controller.cpp
--- a/src/server/src/navigation-controller.cpp
+++ b/src/server/src/navigation-controller.cpp
@@ -335,12 +335,14 @@
 bool NavigationController::windowActivated() { return m_windowActivated; }
 
 void NavigationController::setCloseOnFocusLoss(bool value) { m_closeOnFocusLoss = value; }
 
+bool NavigationController::isTopView(const BaseView *view) const { return topView() == view; }
+
 void NavigationController::setWindowActivated(bool value) {
   if (m_windowActivated == value) return;
 
-  if (!value && m_closeOnFocusLoss) { closeWindow(); }
+  if (!value && m_closeOnFocusLoss && m_focusLossSuppressors == 0) { closeWindow(); }
 
   m_windowActivated = value;
   emit windowActivationChanged(value);
 }
diff --git a/src/server/src/navigation-controller.hpp b/src/server/src/navigation-controller.hpp
--- a/src/server/src/navigation-controller.hpp
+++ b/src/server/src/navigation-controller.hpp
@@ -227,8 +227,21 @@
   void unloadActiveCommand();
 
   void setCloseOnFocusLoss(bool value);
 
+  /** Whether `view` currently sits on top of the navigation stack. */
+  bool isTopView(const BaseView *view) const;
+
+  /**
+   * Suppress close-on-focus-loss while a modal view keeps its own
+   * lifetime (dmenu picker, file chooser). A transient focus change
+   * must not dismiss such a view; release is called when it is gone.
+   */
+  void acquireFocusLossSuppression() { ++m_focusLossSuppressors; }
+  void releaseFocusLossSuppression() {
+    if (m_focusLossSuppressors > 0) { --m_focusLossSuppressors; }
+  }
+
   /**
    * Go back to previous navigation state.
    * If the `instantDismiss` flag is set, this will close the window and pop to root
    * no matter what and then clear the flag.
@@ -277,8 +290,9 @@
   bool m_windowActivated = false;
   bool m_isPanelOpened = false;
   bool m_popToRootOnClose = false;
   bool m_instantDismiss = false;
+  int m_focusLossSuppressors = 0;
   bool m_closeOnFocusLoss = false;
   QWindow *m_window = nullptr;
   std::vector<std::unique_ptr<ViewState>> m_views;
   std::optional<PendingPopToRoot> m_pendingPopToRoot;
diff --git a/src/server/src/ipc-command-server.cpp b/src/server/src/ipc-command-server.cpp
--- a/src/server/src/ipc-command-server.cpp
+++ b/src/server/src/ipc-command-server.cpp
@@ -270,10 +270,10 @@
   static constexpr const int DMENU_SMALL_WIDTH_THRESHOLD = 500;
   auto &nav = m_ctx.navigation;
   auto &cfg = m_ctx.services->config()->value();
 
-  QPromise<ipc_gen::Result<ipc_gen::DMenuResponse>::Type> promise;
-  auto future = promise.future();
+  auto promise = std::make_shared<QPromise<ipc_gen::Result<ipc_gen::DMenuResponse>::Type>>();
+  auto future = promise->future();
 
   if (request.width.has_value() && request.width.value() < DMENU_SMALL_WIDTH_THRESHOLD) {
     qInfo() << "dmenu: disabling quicklook and footer because width is too low";
     request.noFooter = true;
@@ -287,14 +287,31 @@
   using Watcher = QFutureWatcher<ipc_gen::Result<ipc_gen::DMenuResponse>::Type>;
   auto watcher = new Watcher;
   watcher->setFuture(future);
 
-  QObject::connect(watcher, &Watcher::canceled, [nav = nav.get()]() { nav->closeWindow(); });
+  // A stale request (superseded by a newer dmenu invocation) must not
+  // close a window that now belongs to that newer request.
+  QObject::connect(watcher, &Watcher::canceled, [nav = nav.get(), view]() {
+    if (nav->isTopView(view)) { nav->closeWindow(); }
+  });
   QObject::connect(watcher, &Watcher::finished, [watcher]() { watcher->deleteLater(); });
-  QObject::connect(view, &DMenuViewHost::selected,
-                   [promise = std::move(promise)](const QString &text) mutable {
-                     promise.addResult(ipc_gen::DMenuResponse{.output = text.toStdString()});
-                     promise.finish();
+  QObject::connect(view, &DMenuViewHost::selected, view, [promise](const QString &text) {
+    if (promise->future().isFinished()) { return; }
+    promise->addResult(ipc_gen::DMenuResponse{.output = text.toStdString()});
+    promise->finish();
+  });
+  // The picker is modal: a focus flicker must not dismiss it.
+  nav->acquireFocusLossSuppression();
+  QObject::connect(view, &QObject::destroyed, nav.get(),
+                   [nav = nav.get()]() { nav->releaseFocusLossSuppression(); });
+  // Closing the window without a selection (escape / close_window) hides
+  // it without popping the view, so resolve the pending request here too:
+  // otherwise the `vicinae dmenu` client blocks forever in recv().
+  QObject::connect(nav.get(), &NavigationController::windowVisiblityChanged, view,
+                   [promise, nav = nav.get(), view](bool visible) {
+                     if (visible || promise->future().isFinished() || !nav->isTopView(view)) { return; }
+                     promise->addResult(ipc_gen::DMenuResponse{});
+                     promise->finish();
                    });
 
   nav->popToRoot({.clearSearch = false});
   nav->pushView(view);
