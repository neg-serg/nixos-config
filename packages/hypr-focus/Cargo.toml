[package]
name = "hypr-focus"
version = "0.1.0"
edition = "2021"
license = "MIT"

[[bin]]
name = "hypr-focus"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
hyprland = { version = "0.4.0-beta.3", features = ["listener", "dispatch"] }
