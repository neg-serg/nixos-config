--- a/src/tewi/app.py	2026-08-19 09:50:18.955394503 +0300
+++ b/src/tewi/app.py	2026-08-19 09:50:18.962394434 +0300
@@ -25,6 +25,33 @@
 from textual.containers import Horizontal
 from textual.reactive import reactive
 from textual.widgets import ContentSwitcher
+from textual.theme import Theme
+
+# neg.nvim-style pure-dark theme (pure black bg, blue accents, light blue-grey text)
+NEG_THEME = Theme(
+    name="neg",
+    primary="#00508a",
+    secondary="#004a68",
+    accent="#2b6390",
+    success="#0d6b66",
+    warning="#8d7bab",
+    error="#7d2b49",
+    foreground="#5f7086",
+    background="#000000",
+    surface="#000000",
+    dark=True,
+    variables={
+        "panel": "#0a1018",
+        "border": "#121a26",
+        "boost": "#0a1119",
+        "text": "#5f7086",
+        "text-muted": "#4d5c72",
+        "text-disabled": "#323d4a",
+        "text-primary": "#a9bed8",
+        "primary-darken-1": "#00508a",
+        "primary-darken-2": "#00508a",
+    },
+)
 
 from .config import (
     TrackSetAction,
@@ -170,6 +197,10 @@
     ):
         super().__init__()
 
+        # Register and activate the neg pure-dark theme (see NEG_THEME above)
+        self.register_theme(NEG_THEME)
+        self.theme = "neg"
+
         logger.info(f"Initializing Tewi application v{version}")
         logger.info(f"Client configuration: {client_type} at {host}:{port}")
 
@@ -368,6 +399,10 @@
         else:
             self.post_message(Notification("Turtle Mode disabled"))
 
+    def action_toggle_dark(self) -> None:
+        """Toggle between the neg dark theme and the built-in light theme."""
+        self.theme = "textual-light" if self.current_theme.name == "neg" else "neg"
+
     @log_time
     def action_show_statistics(self) -> None:
         self.push_screen(StatisticsDialog(self.client.stats()))
--- a/src/tewi/app.tcss	2026-08-19 09:50:18.984394219 +0300
+++ b/src/tewi/app.tcss	2026-08-19 09:50:18.984394219 +0300
@@ -18,6 +18,13 @@
 
 /* COMMON */
 
+/* Force a solid pure-black app background: otherwise the terminal's native
+   background shows through (kitty blends it with the wallpaper at
+   background_opacity < 1, which reads as grey). */
+Screen {
+    background: #000000;
+}
+
 * {
     scrollbar-size-vertical: 1;
 }
