--- a/src/tewi/app.py	2026-08-19 08:48:05.847792437 +0300
+++ b/src/tewi/app.py	2026-08-19 08:48:05.854792380 +0300
@@ -25,6 +25,32 @@
 from textual.containers import Horizontal
 from textual.reactive import reactive
 from textual.widgets import ContentSwitcher
+from textual.theme import Theme
+
+# neg.nvim-style pure-dark theme (pure black bg, blue accents, light blue-grey text)
+NEG_THEME = Theme(
+    name="neg",
+    primary="#005faf",
+    secondary="#005f87",
+    accent="#367bbf",
+    success="#148787",
+    warning="#c8a8ef",
+    error="#b03362",
+    foreground="#d1e5ff",
+    background="#000000",
+    surface="#121212",
+    dark=True,
+    variables={
+        "panel": "#0d1824",
+        "border": "#1d2433",
+        "boost": "#162b44",
+        "text": "#d1e5ff",
+        "text-muted": "#a5c1e6",
+        "text-disabled": "#7387a1",
+        "primary-darken-1": "#005faf",
+        "primary-darken-2": "#005faf",
+    },
+)
 
 from .config import (
     TrackSetAction,
@@ -170,6 +196,10 @@
     ):
         super().__init__()
 
+        # Register and activate the neg pure-dark theme (see NEG_THEME above)
+        self.register_theme(NEG_THEME)
+        self.theme = "neg"
+
         logger.info(f"Initializing Tewi application v{version}")
         logger.info(f"Client configuration: {client_type} at {host}:{port}")
 
@@ -368,6 +398,10 @@
         else:
             self.post_message(Notification("Turtle Mode disabled"))
 
+    def action_toggle_dark(self) -> None:
+        """Toggle between the neg dark theme and the built-in light theme."""
+        self.theme = "textual-light" if self.current_theme.name == "neg" else "neg"
+
     @log_time
     def action_show_statistics(self) -> None:
         self.push_screen(StatisticsDialog(self.client.stats()))
