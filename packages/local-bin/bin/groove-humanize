#!/bin/sh
# groove-humanize: humanize a drum MIDI loop with the GrooVAE model
# (torch port of the magenta GrooVAE; official TF1 weights converted).
# Usage: groove-humanize input.mid -o output.mid [--tempo 120]
set -eu
export LD_LIBRARY_PATH="/nix/store/7vafhlh0lmcvi75jfyy09qwr4m3x1ks3-gcc-15.2.0-lib/lib:/nix/store/483x61iy35irm4wr2b7dwzihljhp6da2-zlib-1.3.2/lib:${LD_LIBRARY_PATH:-}"
exec "$HOME/src/music-ai/venv-groovae/bin/python" "$HOME/src/music-ai/groovae/groove_humanize.py" "$@"
